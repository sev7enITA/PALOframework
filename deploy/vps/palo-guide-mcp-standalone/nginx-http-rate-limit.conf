# Install in /etc/nginx/conf.d/palo-guide-mcp-rate-limit.conf.
# This directive belongs in the nginx http context, not inside a server block.
limit_req_zone $binary_remote_addr zone=palo_guide_mcp:10m rate=60r/m;
