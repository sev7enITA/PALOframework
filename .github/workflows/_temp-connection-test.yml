name: Temporary connection test

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/_temp-connection-test.yml

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check runner
        run: echo "GitHub Actions available"
